# Complete v1 internal TestFlight pass

The complete-v1 branch intentionally bundles the production-direction Family Inbox workflow into one internal TestFlight build so runtime/device defects can be found together instead of through many tiny builds.

The trust boundary remains unchanged: imported/OCR/AI data becomes editable proposals first. Nothing becomes canonical Plan data until the user explicitly confirms it.
