name: Rapport AI Validate

on:
  workflow_dispatch:
  push:
    paths:
      - "apps/012-rapport-ai/**"
      - ".github/workflows/rapport-ai-validate.yml"
  pull_request:
    paths:
      - "apps/012-rapport-ai/**"
      - ".github/workflows/rapport-ai-validate.yml"

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Inspect project
        run: xcodebuild -project apps/012-rapport-ai/prototype/RapportAI.xcodeproj -scheme RapportAI -list
      - name: Build iPhone simulator
        run: |
          set -o pipefail
          xcodebuild \
            -project apps/012-rapport-ai/prototype/RapportAI.xcodeproj \
            -scheme RapportAI \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO \
            build | xcpretty
      - name: Validate privacy and secrets boundary
        run: |
          test -f apps/012-rapport-ai/prototype/RapportAI/PrivacyInfo.xcprivacy
          ! rg -n 'sk-[A-Za-z0-9_-]{20,}' apps/012-rapport-ai
          rg -n 'OPENAI_API_KEY' apps/012-rapport-ai/supabase/functions/generate-rapport/index.ts

